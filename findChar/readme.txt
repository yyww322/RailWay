软件功能：将字库中的文本库表读出，得到对应的代码，并生成对应的命令
说明：
1先将excel的文件弄成仅有三个竖列的形式
2把excel另存为.txt的格式
3 使用文本编辑器将txt文件另存为utf-8的格式，否则python不能识别
4 1-3步仅在每次修改了库后才需要重新导出


使用方法： python work.py 需要转换的字串 (字库名.txt ，不输入默认为workChar.txt)